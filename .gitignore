# geliştirme / hata ayıklama render çıktıları
models/_*.png

# 3D model referans görselleri (büyük renderlar, yerelde kalsın — repoyu şişirmesin)
models/*.glb.jpg

# yedek blend
*.blend1

# geçici
*.tmp
.DS_Store
Thumbs.db

# Claude yerel ayarları (makineye özel)
.claude/settings.local.json
